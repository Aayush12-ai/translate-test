{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"API_PORT=18080 pnpm --filter @workspace/api-server dev\" \"PORT=5000 API_PROXY_TARGET=http://127.0.0.1:18080 pnpm --filter @workspace/video-call dev\"",
    "dev:api": "API_PORT=18080 pnpm --filter @workspace/api-server dev",
    "dev:web": "PORT=5000 API_PROXY_TARGET=http://127.0.0.1:18080 pnpm --filter @workspace/video-call dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
